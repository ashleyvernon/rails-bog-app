<h1>New Creature</h1>
<hr>
<%= form_for @creature do |f| %>
	<div class="form-group">
		<%= f.text_field :name %>
	</div>	
	<div class="form-group">	
		<%= f.text_area :description %>
	</div>
	<div class="form-group">
		<%= f.submit "Save Creature" %>
	</div>
<% end %>